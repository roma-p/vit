# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='vit',
    version='0.1.0',
    description='vfx information tracker',
    author='roma_p',
    url='https://github.com/roma-p/vit',
    packages=find_packages(exclude=('tests',))
)